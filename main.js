const { createApp } = Vue;
const app = createApp({
    data() {
        //console.log(catalogo);
        return{
            players:catalogo
        }
    },
    mounted() {
    },
    methods: {
    }
}).mount('#app');
