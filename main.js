// setting up the vue instance

const { createApp } = Vue;
const app = createApp({
    data() {
        return {
            players: catalogo
        }
    },
    mounted() {
    },
    methiods: {

    }

}).mount("#app")
